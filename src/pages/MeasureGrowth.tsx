import { Measure } from "../components/Measure";

/**
 *
 * @returns {React.JSX.Element} This page lets you measure the precise amount
 * of forward growth of a patient's face. Can scale and rotate patient image
 * as needed to get a precise fit to the Growth Guide
 */
export const MeasureGrowth = (): React.JSX.Element => {
  return <Measure />;
};
